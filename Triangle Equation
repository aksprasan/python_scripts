height=input('What is the height of the triangle: ')
length=input('What is the length of the triangle: ')
area =  float(height) * float(length)/2
d=float(height)**2 + float(length)**2
dia=d**0.5
print('The area of your triangle is',area, 'and the diagonal is', dia)
